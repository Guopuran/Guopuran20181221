<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ImageView
        android:id="@+id/change_image"
        android:layout_width="150dp"
        android:layout_height="100dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_margin="5dp"
        />
    <TextView
        android:id="@+id/change_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        app:layout_constraintLeft_toLeftOf="@id/change_image"
        app:layout_constraintRight_toRightOf="@id/change_image"
        app:layout_constraintTop_toBottomOf="@id/change_image"
        />
    <TextView
        android:id="@+id/change_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="@id/change_image"
        app:layout_constraintRight_toRightOf="@id/change_image"
        app:layout_constraintTop_toBottomOf="@id/change_title"
        android:layout_margin="5dp"
        android:textSize="18sp"
        android:textColor="#ff0000"
        />
</android.support.constraint.ConstraintLayout>